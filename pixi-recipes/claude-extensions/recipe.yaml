package:
  name: claude-extensions
  version: "1"

source:
  - path: .

requirements:
  host:
    - nodejs
    - rtk-cli
  run:
    - nodejs
    - rtk-cli

build:
  number: 0
  script:
    # Extension-less: resolves to build.sh on Linux and build.bat on Windows
    file: build
