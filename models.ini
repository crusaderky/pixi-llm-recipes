version = 1

[*]
jinja = true
# Increase log-verbosity to 5 to see the raw RPC chatter between server and harness
log-verbosity = 4
flash-attn = on
models-max = 1

# Setting informed by https://github.com/ggml-org/llama.cpp/pull/7412#issuecomment-2120427347
# TODO: Change to Turboquant once merged into llama.cpp main branch
cache-type-k = q8_0
cache-type-v = q8_0
# Almost half the size and the degradation from q8_0 is not measurable with
# llama-perplexity, but the whole internet says they're terrible?!?
; cache-type-k = q4_0
; cache-type-v = q4_0

[Qwen3.6-35B-A3B]
# The best quality model that is performant on less
# than 24 GiB VRAM, as of 2026-06-04

# 18 GiB, 98.71% accuracy, 56±5 tok/s
hf = byteshape/Qwen3.6-35B-A3B-MTP-GGUF:Qwen3.6-35B-A3B-IQ4_XS-3.97bpw

# Choose a context size
; ctx-size = 131072  # 128k recommended on 8 GiB VRAM
ctx-size = 262144  # 256k recommended on 10 GiB+ VRAM

# Never touch ngl or speed will drop drastically
ngl = 99
# Progressively reduce n-cpu-moe from 40 to 0 until you get memory errors
# (the smaller the ctx-size the more experts you can offload to GPU)
n-cpu-moe = 40

# Alternatively, try an automatic fit by commenting out ngl and n-cpu-moe above and
# uncomment fit-target below. Increase fit-target if llama.cpp crashes with
# out-of-memory errors.
; fit-target = 0
# Look for these lines in the log file:
# [49325] 0.06.892.992 I common_params_fit_impl:   - CUDA0 (NVIDIA GeForce RTX 3080): 42 layers (40 overflowing),   6208 MiB used,    918 MiB free
# [49325] 0.17.069.257 I load_tensors: offloaded 42/42 layers to GPU

# Uncomment if your workflow is very varied in domain - which will increase the number
# of experts active at once. For coding on a single project, mmap is a lot less host
# RAM-intensive and may be slightly faster on CUDA. On Vulkan, no-mmap gives a 50% speed
# boost to input tokens! YMMV. Test with llama-benchy and don't forget that a warm
# server is faster than a cold one.
; no-mmap = true

# Suppress Qwen-specific warning
image-min-tokens = 1024  

# Enable MTP
spec-type = draft-mtp
spec-draft-n-max = 3

# Comment out to move mmproj to VRAM for faster image analysis
no-mmproj-offload = true

# Prevent infinite loops in the inner monologue
# https://whamp.github.io/blog/qwen-3-6-27b-livecodebench-reasoning-budget/
reasoning-budget = 8192
reasoning-budget-message = "My reasoning budget is exhausted, but I have enough information to answer directly now.</think>"

# Choose one set of parameters depending on usage
# From https://huggingface.co/unsloth/Qwen3.6-35B-A3B-MTP-GGUF

# Thinking mode for general tasks
; temperature = 1.0
; top_p = 0.95
; top_k = 20
; min_p = 0.0
; presence_penalty = 1.5
; repeat_penalty = 1.0
; # preserve_thinking makes the context baloon and is
; # harmful when the KV cache is quantized
; chat-template-kwargs = {"preserve_thinking":false}

# Thinking mode for precise coding tasks 
temperature = 0.6
top-p = 0.95
top-k = 20
min-p = 0.0
presence-penalty = 0.0
repeat-penalty = 1.0
# preserve_thinking makes the context baloon and is
# harmful when the KV cache is quantized
chat-template-kwargs = {"preserve_thinking":false}

# Instruct (or non-thinking) mode for general tasks
; temperature = 0.7
; top-p = 0.8
; top-k = 20
; min-p = 0.0
; presence-penalty = 1.5
; repeat-penalty = 1.0
; chat-template-kwargs = {"enable_thinking":false}

# Instruct (or non-thinking) mode for reasoning tasks
; temperature = 1.0
; top-p = 0.95
; top-k = 20
; min-p = 0.0
; presence-penalty = 1.5
; repeat-penalty = 1.0
; chat-template-kwargs = {"enable_thinking":false}

[MiniCPM5-1B]
# 0.7 GiB, 455±37 tok/s
# FIXME Tool use not compatible with llamacpp
# https://github.com/ggml-org/llama.cpp/pull/23802
hf = openbmb/MiniCPM5-1B-GGUF:Q4_K_M
ctx-size = 131072
ngl = 99

# Think
reasoning = on
temperature = 0.9
top-p = 0.95

# No Think
; reasoning = off
; temperature = 0.7
; top-p = 0.95

[Gemma4-E2B]
# 3.5 GiB multimodal, 280±23 tok/s
hf = unsloth/gemma-4-E2B-it-qat-GGUF:UD-Q4_K_XL
ctx-size = 131072
ngl = 99

temperature = 1.0
top-p = 0.95
top-k = 64
; chat-template-kwargs = {"enable_thinking":false}

# MTP drafter (download with `pixi r download-gemma-drafters`)
md = huggingface/hub/models--unsloth--gemma-4-E2B-it-qat-GGUF/blobs/8702bf70ab5604dcc818f26ea144fd4237c9908c15992d5b34746c65039dc65d
spec-type = draft-mtp
spec-draft-n-max = 3
spec-draft-ngl = 99

[Gemma4-E4B]
# 5.0 GiB multimodal, 137±5 tok/s
hf = unsloth/gemma-4-E4B-it-qat-GGUF:UD-Q4_K_XL
ctx-size = 131072
ngl = 99

temperature = 1.0
top-p = 0.95
top-k = 64
; chat-template-kwargs = {"enable_thinking":false}

# MTP drafter (download with `pixi r download-gemma-drafters`)
# FIXME broken
; md = huggingface/hub/models--unsloth--gemma-4-E4B-it-qat-GGUF/blobs/b0005dc39d47ede950c3ec413cb20e832f15b216126eae368d9f572676153cb6
; spec-type = draft-mtp
; spec-draft-n-max = 3
; spec-draft-ngl = 99

[Gemma4-12B]
# 6.7 GiB multimodal, 109±8 tok/s
hf = unsloth/gemma-4-12B-it-qat-GGUF:UD-Q4_K_XL
ngl = 99

# Comment out to move mmproj to VRAM for faster image analysis
no-mmproj-offload = true

ctx-size = 32768  # Most that can fit in 10 GiB
; ctx-size = 65536
; ctx-size = 131072
; ctx-size = 262144  # Largest supported context

temperature = 1.0
top-p = 0.95
top-k = 64
; chat-template-kwargs = {"enable_thinking":false}

# MTP drafter (download with `pixi r download-gemma-drafters`)
md = huggingface/hub/models--unsloth--gemma-4-12B-it-qat-GGUF/blobs/c50c91c35f04903815b2e8930cbb8c8c5bee0e1aa00748c30a7b8ff05d2310b4
spec-type = draft-mtp
spec-draft-n-max = 3
spec-draft-ngl = 99

[Gemma4-31B]
# 18 GiB multimodal; needs 32 GiB VRAM
hf = unsloth/gemma-4-31B-it-qat-GGUF:UD-Q4_K_XL
# Comment out to run (glacially) on less than 32 GiB VRAM
ngl = 99

# Uncomment to spill mmproj to host RAM
; no-mmproj-offload = true

; ctx-size = 262144  # Needs 12.5 GiB VRAM alone; MTP drafter is capped at 128k
ctx-size = 131072  # 7.5 GiB

temperature = 1.0
top-p = 0.95
top-k = 64
; chat-template-kwargs = {"enable_thinking":false}

# MTP drafter (download with `pixi r download-gemma-drafters`)
md = huggingface/hub/models--unsloth--gemma-4-31B-it-qat-GGUF/blobs/b5c4e583fc5982439080114bbc1b7edaec361f9d4c9193d6bed606a3de401b62
spec-type = draft-mtp
spec-draft-n-max = 3
spec-draft-ngl = 99
